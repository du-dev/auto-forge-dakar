[build]
  command = "npm run build"
  publish = "dist"
  installCommand = "npm install --legacy-peer-deps"

# Fonction serverless pour la reconnaissance vocale Whisper
[functions]
  directory = "netlify/functions"
